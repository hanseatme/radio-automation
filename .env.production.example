# ============================================
# RadioPro - Produktions-Konfiguration
# ============================================
# Kopiere diese Datei nach .env und passe die Werte an!

# === PORTS ===
# Web Admin Interface (Standard: 8080)
WEB_PORT=8080

# Icecast Stream Port fuer Listener (Standard: 8000)
STREAM_PORT=8000

# Mikrofon-Input Port fuer Live-Moderation (Standard: 9999)
MIC_PORT=9999

# === PFADE (WICHTIG!) ===
# Absoluter Pfad zum Medien-Verzeichnis auf dem Server
# Beispiel: /var/www/vhosts/meinedomain.de/radio/media
MEDIA_PATH=/pfad/zum/media/ordner

# Absoluter Pfad zum Daten-Verzeichnis (Datenbank)
# Beispiel: /var/www/vhosts/meinedomain.de/radio/data
DATA_PATH=/pfad/zum/data/ordner

# === SICHERHEIT (UNBEDINGT AENDERN!) ===
# Geheimer Schluessel fuer Session-Verschluesselung
# Generiere einen zufaelligen String, z.B. mit: openssl rand -hex 32
SECRET_KEY=HIER_EINEN_LANGEN_ZUFAELLIGEN_STRING_EINTRAGEN

# Admin-Zugangsdaten fuer das Web-Interface
ADMIN_USERNAME=admin
ADMIN_PASSWORD=SICHERES_PASSWORT_HIER

# === ZEITZONE ===
TIMEZONE=Europe/Berlin
